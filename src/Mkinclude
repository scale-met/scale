ifeq ($(origin BINDIR), undefined)
	BINDIR = $(TOPDIR)/bin/$(SCALE_SYS)
endif

ifeq ($(origin LIBDIR), undefined)
   LIBDIR = $(TOPDIR)/lib
endif

GTOOLDIR   = $(TOPDIR)/gtool
DCUTILSDIR = $(TOPDIR)/dc_utils
SRCDIR     = $(TOPDIR)/src
INCDIR     = $(TOPDIR)/src/include

SUFFIX = $(index)_$(precision)_$(dynamics)_$(tracer)_$(sfcflx)_$(radiation)_$(turbulence)_$(postfix)

LIBNAME = libscale_$(SUFFIX).a

BINNAME  = scale3_$(SUFFIX)
INITNAME = scale3_init_$(SUFFIX)

ifeq ($(RDMA),-D_USE_RDMA)
mod_rdma := rdma.o
else
mod_rdma :=
endif

TESTDIR = $(TOPDIR)/test
